{
  "name": "spec-tools-mcp",
  "version": "1.4.0",
  "description": "Spec-driven toolkit for AI agents — skills, rules & prompts via MCP",
  "mcpServers": {
    "spec-tools-mcp": {
      "command": "npx",
      "args": [
        "-y",
        "spec-tools-mcp@latest"
      ]
    }
  }
}
